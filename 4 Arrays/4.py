# Declare an array with values "Peter", "Ann", and "Paula". In interactive mode, display:

names = ["Peter", "Ann", "Paula"]

# a. The number of names in the array
print(len(names))

# b. First name
print(names[0])


# c. Last name (by calculating the number of array elements)
print(names[len(names) - 1])